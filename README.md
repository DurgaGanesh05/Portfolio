
  # Design

  This is a code bundle for Design. The original project is available at https://www.figma.com/design/UyeDBbhgfl5NUr6toCmI8E/Design.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  